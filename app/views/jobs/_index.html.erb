<%= form_tag '/jobs' do |f| %>
  <%= select_tag "jobs", options_for_select(@jobs.map {|j| j.title}), class: "btn btn-default btn-lg dropdown-toggle" %>
  <%= text_field_tag "worker", value =  nil, id: 'worker-search' %>
  <%= submit_tag %>
<% end %>
