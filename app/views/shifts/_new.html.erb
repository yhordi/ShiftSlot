<%= form_tag do |f| %>
  <%= select_tag @jobs, options_for_select(jobs.map {|j| [j.title, j.id]}), class: "btn btn-default btn-lg dropdown-toggle "  %>
  <%= submit_tag 'Create Shift', class: 'btn btn-default btn-success'%>
<% end %>
